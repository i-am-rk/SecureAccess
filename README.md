# SecureAccess
A full-stack authentication and authorization demo built with React (frontend) and Spring Boot (backend). This project showcases secure login, JWT-based authentication, and role-based authorization (RBAC).
